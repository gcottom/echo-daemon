save_dir: ./data # If using Docker, don't change this. If running via script, you can change to your desired directory
temp_dir: ./temp # If using Docker, don't change this. If running via script, you can change to your desired directory
music_dir: ./music # The directory where your music files are stored. If using Docker, this should be the path inside the container.
spotify_client_id:  # Your Spotify client ID, acquired from the Spotify Developer Dashboard
spotify_client_secret:  # Your Spotify client secret, acquired from the Spotify Developer Dashboard
